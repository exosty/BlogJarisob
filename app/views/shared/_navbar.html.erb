<nav class="navbar navbar-default navbar-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand">Blog JarisoB</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">

        <li><%= link_to 'All posts', root_url %></li>
        <li><%= link_to 'All tags', tags_path %></li>
        <% if user_signed_in? %>
                <li><%= link_to 'My favorite', favorite_path%></li>
        <% end %>
        <% if user_signed_in? %>
            <% if can? :create, Post %>
              <li><%= link_to 'New Post', new_post_path %></li>
            <% end %>
        <% end %>
      </ul>

      <ul class="nav navbar-nav navbar-text">

        <% if user_signed_in? %>
            <span class="text-success" >Welcome, <b><%= current_user.email %></b>!</span>
            <% else %>
            <span class="text-danger">Welcome, you are not logged in!</span>
      <% end %>
      </ul>

      <ul class="nav navbar-nav navbar-right">
          <% if user_signed_in? %>
            <a href="/users/sign_out" rel="nofollow" data-method="delete" class="btn btn-default navbar-btn btn-danger">Log out</a>
        <% else %>
            <a href="/users/sign_in" rel="nofollow" class="btn btn-default navbar-btn btn-success">Sign in</a>
        <% end %>
        </button>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
